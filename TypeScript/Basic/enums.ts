// Enum -> Enumeration

enum seatChoice {
    A = 0, // telling the seat number from ehre it start
    M = 20, // now middle start with 20
    W, // not assigned ny so window has 21 
}

const akgSeat = seatChoice.W